import React, { Fragment } from 'react'
import AuthGuard from '../../shared/guards/AuthGuard'

function Disconnect() {
  return (
    <Fragment>

    </Fragment>
  )
}

export default AuthGuard(Disconnect)